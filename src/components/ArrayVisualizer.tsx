import { motion } from "framer-motion"; import { useSortingStore } from "../store/sortingStore";
export function ArrayVisualizer(){const array=useSortingStore(s=>s.array),active=useSortingStore(s=>s.activeIndices),sorted=useSortingStore(s=>s.sortedIndices),pivot=useSortingStore(s=>s.pivotIndex),range=useSortingStore(s=>s.activeRange); const max=Math.max(1,...array.map(Math.abs)); return <div className="visualizer" aria-label="Animated sorting array">{array.map((value,index)=><div className={`bar-wrap ${range&&index>=range[0]&&index<=range[1]?"in-range":""}`} key={`${index}-${value}`}><motion.div layout className={`bar ${sorted.includes(index)?"sorted":""} ${active.includes(index)?"active":""} ${pivot===index?"pivot":""}`} animate={{height:`${Math.max(8,Math.abs(value)/max*100)}%`}} title={`Index ${index}: ${value}`}><span>{value}</span></motion.div><small>{index}</small></div>)}</div>}