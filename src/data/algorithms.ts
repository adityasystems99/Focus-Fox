import type { AlgorithmId, SortingAlgorithm } from "../types/sorting";
import bubble from "../algorithms/bubbleSort"; import selection from "../algorithms/selectionSort"; import insertion from "../algorithms/insertionSort"; import merge from "../algorithms/mergeSort"; import quick from "../algorithms/quickSort"; import heap from "../algorithms/heapSort"; import counting from "../algorithms/countingSort"; import radix from "../algorithms/radixSort"; import bucket from "../algorithms/bucketSort"; import shell from "../algorithms/shellSort"; import cocktail from "../algorithms/cocktailSort"; import comb from "../algorithms/combSort"; import gnome from "../algorithms/gnomeSort"; import cycle from "../algorithms/cycleSort";
const definitions: [AlgorithmId, string, string, string, string, string, string][] = [
  ["bubble","Bubble Sort","Basic","Repeatedly swaps adjacent out-of-order values.","O(n^2)","O(1)","Stable"],["selection","Selection Sort","Basic","Selects the smallest remaining value for each position.","O(n^2)","O(1)","Not stable"],["insertion","Insertion Sort","Basic","Builds a sorted prefix by inserting each next value.","O(n^2)","O(1)","Stable"],["merge","Merge Sort","Divide & Conquer","Recursively splits and merges ordered ranges.","O(n log n)","O(n)","Stable"],["quick","Quick Sort","Divide & Conquer","Partitions values around a selected pivot.","O(n log n)","O(log n)","Not stable"],["heap","Heap Sort","Heap","Builds a max heap and extracts its root.","O(n log n)","O(1)","Not stable"],["counting","Counting Sort","Non-Comparison","Counts occurrences across an integer range.","O(n + k)","O(k)","Stable"],["radix","Radix Sort","Non-Comparison","Processes non-negative values one digit at a time.","O(dn)","O(n)","Stable"],["bucket","Bucket Sort","Non-Comparison","Distributes values into sorted range buckets.","O(n + k)","O(n + k)","Stable"],["shell","Shell Sort","Advanced","Insertion sort over progressively smaller gaps.","O(n log n)","O(1)","Not stable"],["cocktail","Cocktail Shaker Sort","Advanced","Bubbles large values right and small values left.","O(n^2)","O(1)","Stable"],["comb","Comb Sort","Advanced","Uses a shrinking gap to remove turtles.","O(n^2)","O(1)","Not stable"],["gnome","Gnome Sort","Advanced","Walks backward after each misplaced value.","O(n^2)","O(1)","Stable"],["cycle","Cycle Sort","Advanced","Minimizes writes by placing values in cycles.","O(n^2)","O(1)","Not stable"]
];
const generators = { bubble, selection, insertion, merge, quick, heap, counting, radix, bucket, shell, cocktail, comb, gnome, cycle };
const pseudocodeFor = (id: AlgorithmId, name: string) => {
  const lines: Record<AlgorithmId, string[]> = {
    bubble: ["for each unsorted suffix", "compare adjacent values", "swap when left is greater"], selection: ["for each position", "find the minimum remaining value", "swap it into position"], insertion: ["take the next value", "shift larger values right", "insert the value"], merge: ["split the active range", "merge two sorted halves", "overwrite the range in order"], quick: ["select a pivot", "partition the active range", "recurse on both sides"], heap: ["build a max heap", "extract the root", "heapify the remaining range"], counting: ["count each value", "walk the frequency table", "write values in order"], radix: ["select a digit place", "distribute into digit buckets", "collect buckets"], bucket: ["distribute values into buckets", "sort each bucket", "extract buckets in order"], shell: ["select a shrinking gap", "compare gap-separated values", "insert into the gap-sorted range"], cocktail: ["scan left to right", "scan right to left", "swap adjacent inversions"], comb: ["select a shrinking gap", "compare gap-separated values", "swap inverted pairs"], gnome: ["compare neighboring values", "step back after a swap", "step forward when ordered"], cycle: ["find the value's destination", "rotate the cycle", "repeat until placed"]
  };
  return [...lines[id], "update the visible state", "mark the completed range"].map((text, index) => ({ line: index + 1, text }));
};
export const algorithms: SortingAlgorithm[] = definitions.map(([id,name,category,description,complexity,space,stable]) => ({ id,name,category,description,complexity:{best: complexity, average: complexity, worst: complexity, space, stable, inPlace: space === "O(1)" ? "Yes" : "No"}, pseudocode:pseudocodeFor(id, name), generateSteps: (input) => generators[id](input) }));
export const algorithmMap = Object.fromEntries(algorithms.map((algorithm) => [algorithm.id, algorithm])) as Record<AlgorithmId, SortingAlgorithm>;